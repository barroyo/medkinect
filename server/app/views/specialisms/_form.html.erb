<%= form_for(@specialism) do |f| %>
  <% if @specialism.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@specialism.errors.count, "error") %> prohibited this specialism from being saved:</h2>

      <ul>
      <% @specialism.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
